class Movie:

    def __init__(self, ID, name, scenes, characters, genres):
        self.__movieID = ID
        self.__movieName = name
        self.__conversation = scenes
        self.__characters = characters
        self.__genres = genres
